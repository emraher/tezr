% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/session.R
\name{request_config}
\alias{request_config}
\title{Configure request behavior}
\usage{
request_config(user_agent = NULL, verbose = NULL, reset = FALSE)
}
\arguments{
\item{user_agent}{Character. Optional user agent to send with HTTP requests.}

\item{verbose}{Logical. Show informational request messages?}

\item{reset}{Logical. Reset request options to package defaults before
applying other arguments.}
}
\value{
Invisible list with the active request configuration.
}
\description{
Sets package-level request options. \code{tezr} identifies itself with a
package-specific user agent by default. You can override that header when
required by a network policy or by the National Thesis Center portal.
}
\details{
Informational messages are shown by default. Set \code{verbose = FALSE} to
silence progress and success messages. Warnings and errors are still shown.

You can also set \code{TEZR_USER_AGENT} and \code{TEZR_VERBOSE} before starting R.
Explicit R options take precedence over environment variables.
}
\examples{
# Silence progress messages for the current R session
request_config(verbose = FALSE)

# Set a custom user agent for an institutional network policy
request_config(user_agent = "my-lab-contact@example.edu")

# Return to package defaults
request_config(reset = TRUE)
}
\concept{request configuration}
